export {};

// import { Structure } from "../classes/Classes";
// import { CollectionTypes } from "./../types/types.d";

// export const constructClass = (type: CollectionTypes, props?: any) => {
//   switch (type) {
//     case CollectionTypes.STRUCTURES:
//       return new Structure(props);
//     default:
//       break;
//   }
// };
