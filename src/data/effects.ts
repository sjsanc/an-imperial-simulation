import { EffectProps } from "../classes/Effect";

const effects: EffectProps[] = [
  {
    name: "raisePopCap",
    description: "Raise the population cap by an amount",
    message: "Increasing <strong>population cap</strong> by <span class='increase'>+{{0}}</span>",
  },
];

export default effects;
